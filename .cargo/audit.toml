[advisories]
# atomic-polyfill (RUSTSEC-2023-0089) is an unmaintained transitive dep pulled in
# via postcard → heapless v0.7. heapless v0.8+ dropped it, but postcard constrains
# the version. Nothing actionable until postcard upgrades. Suppressed for now.
ignore = ["RUSTSEC-2023-0089"]
